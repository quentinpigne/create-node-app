const { Container } = require('typedi');

class HelloController {
  hello() {
    return { message: 'Hello world!' };
  }
}

Container.set('hello.controller', new HelloController());

module.exports = Container.get('hello.controller');
