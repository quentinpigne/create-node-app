import Container from 'typedi';
{{> (importsPartial)}}

import { HelloController } from '@controllers/hello.controller';

const helloController: HelloController = Container.get(HelloController);

export default {{> (routesPartial)}}
