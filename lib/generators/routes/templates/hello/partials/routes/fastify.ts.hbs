(fastify: FastifyInstance, opts: RegisterOptions, done: () => void) => {
  fastify.get('/', (request: FastifyRequest, reply: FastifyReply) => (void reply.send(helloController.hello())));
  done();
};
