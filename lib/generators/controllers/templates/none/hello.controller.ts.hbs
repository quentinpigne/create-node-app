import { IncomingMessage, ServerResponse } from 'http';

export const hello = (request: IncomingMessage, response: ServerResponse) => {
  response.end(JSON.stringify({ message: 'Hello world!' }, null, 2));
};
